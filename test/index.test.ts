import { toRoman } from '../src';

describe('Roman Converter', () => {
  it('should convert 1 to I', () => {
    // arrange
    const number = 1;

    // act
    const want = 'I';
    const have = toRoman(number);

    // assert
    expect(have).toEqual(want);
  });

  it('should convert 2 to II', () => {
    // arrange
    const number = 2;

    // act
    const want = 'II';
    const have = toRoman(number);

    // assert
    expect(have).toEqual(want);
  });

  it('should convert 3 to III', () => {
    // arrange
    const number = 3;

    // act
    const want = 'III';
    const have = toRoman(number);

    // assert
    expect(have).toEqual(want);
  });

  it('should convert 4 to IV', () => {
    // arrange
    const number = 4;

    // act
    const want = 'IV';
    const have = toRoman(number);

    // assert
    expect(have).toEqual(want);
  });

  it('should convert 5 to V', () => {
    // arrange
    const number = 5;

    // act
    const want = 'V';
    const have = toRoman(number);

    // assert
    expect(have).toEqual(want);
  });

  it('should convert 6 to VI', () => {
    // arrange
    const number = 6;

    // act
    const want = 'VI';
    const have = toRoman(number);

    // assert
    expect(have).toEqual(want);
  });

  it('should convert 7 to VII', () => {
    // arrange
    const number = 7;

    // act
    const want = 'VII';
    const have = toRoman(number);

    // assert
    expect(have).toEqual(want);
  });

  it('should convert 8 to VIII', () => {
    // arrange
    const number = 8;

    // act
    const want = 'VIII';
    const have = toRoman(number);

    // assert
    expect(have).toEqual(want);
  });

  it('should convert 9 to IX', () => {
    // arrange
    const number = 9;

    // act
    const want = 'IX';
    const have = toRoman(number);

    // assert
    expect(have).toEqual(want);
  });

  it('should convert 10 to X', () => {
    // arrange
    const number = 10;

    // act
    const want = 'X';
    const have = toRoman(number);

    // assert
    expect(have).toEqual(want);
  });

  it('should convert 11 to XI', () => {
    // arrange
    const number = 11;

    // act
    const want = 'XI';
    const have = toRoman(number);

    // assert
    expect(have).toEqual(want);
  });

  it('should convert 12 to XII', () => {
    // arrange
    const number = 12;

    // act
    const want = 'XII';
    const have = toRoman(number);

    // assert
    expect(have).toEqual(want);
  });

  it('should convert 13 to XIII', () => {
    // arrange
    const number = 13;

    // act
    const want = 'XIII';
    const have = toRoman(number);

    // assert
    expect(have).toEqual(want);
  });

  it('should convert 14 to XIV', () => {
    // arrange
    const number = 14;

    // act
    const want = 'XIV';
    const have = toRoman(number);

    // assert
    expect(have).toEqual(want);
  });

  it('should convert 15 to XV', () => {
    // arrange
    const number = 15;

    // act
    const want = 'XV';
    const have = toRoman(number);

    // assert
    expect(have).toEqual(want);
  });

  it('should convert 16 to XVI', () => {
    // arrange
    const number = 16;

    // act
    const want = 'XVI';
    const have = toRoman(number);

    // assert
    expect(have).toEqual(want);
  });

  it('should convert 17 to XVII', () => {
    // arrange
    const number = 17;

    // act
    const want = 'XVII';
    const have = toRoman(number);

    // assert
    expect(have).toEqual(want);
  });

  it('should convert 18 to XVIII', () => {
    // arrange
    const number = 18;

    // act
    const want = 'XVIII';
    const have = toRoman(number);

    // assert
    expect(have).toEqual(want);
  });

  it('should convert 19 to XIX', () => {
    // arrange
    const number = 19;

    // act
    const want = 'XIX';
    const have = toRoman(number);

    // assert
    expect(have).toEqual(want);
  });

  it('should convert 20 to XX', () => {
    // arrange
    const number = 20;

    // act
    const want = 'XX';
    const have = toRoman(number);

    // assert
    expect(have).toEqual(want);
  });

  it('should convert 21 to XXI', () => {
    // arrange
    const number = 21;

    // act
    const want = 'XXI';
    const have = toRoman(number);

    // assert
    expect(have).toEqual(want);
  });

  it('should convert 22 to XXII', () => {
    // arrange
    const number = 22;

    // act
    const want = 'XXII';
    const have = toRoman(number);

    // assert
    expect(have).toEqual(want);
  });

  it('should convert 23 to XXIII', () => {
    // arrange
    const number = 23;

    // act
    const want = 'XXIII';
    const have = toRoman(number);

    // assert
    expect(have).toEqual(want);
  });

  it('should convert 24 to XXIV', () => {
    // arrange
    const number = 24;

    // act
    const want = 'XXIV';
    const have = toRoman(number);

    // assert
    expect(have).toEqual(want);
  });

  it('should convert 25 to XXV', () => {
    // arrange
    const number = 25;

    // act
    const want = 'XXV';
    const have = toRoman(number);

    // assert
    expect(have).toEqual(want);
  });

  it('should convert 26 to XXVI', () => {
    // arrange
    const number = 26;

    // act
    const want = 'XXVI';
    const have = toRoman(number);

    // assert
    expect(have).toEqual(want);
  });

  it('should convert 27 to XXVII', () => {
    // arrange
    const number = 27;

    // act
    const want = 'XXVII';
    const have = toRoman(number);

    // assert
    expect(have).toEqual(want);
  });

  it('should convert 28 to XXVIII', () => {
    // arrange
    const number = 28;

    // act
    const want = 'XXVIII';
    const have = toRoman(number);

    // assert
    expect(have).toEqual(want);
  });

  it('should convert 29 to XXIX', () => {
    // arrange
    const number = 29;

    // act
    const want = 'XXIX';
    const have = toRoman(number);

    // assert
    expect(have).toEqual(want);
  });

  it('should convert 30 to XXX', () => {
    // arrange
    const number = 30;

    // act
    const want = 'XXX';
    const have = toRoman(number);

    // assert
    expect(have).toEqual(want);
  });

  it('should convert 31 to XXXI', () => {
    // arrange
    const number = 31;

    // act
    const want = 'XXXI';
    const have = toRoman(number);

    // assert
    expect(have).toEqual(want);
  });

  it('should convert 32 to XXXII', () => {
    // arrange
    const number = 32;

    // act
    const want = 'XXXII';
    const have = toRoman(number);

    // assert
    expect(have).toEqual(want);
  });

  it('should convert 33 to XXXIII', () => {
    // arrange
    const number = 33;

    // act
    const want = 'XXXIII';
    const have = toRoman(number);

    // assert
    expect(have).toEqual(want);
  });

  it('should convert 34 to XXXIV', () => {
    // arrange
    const number = 34;

    // act
    const want = 'XXXIV';
    const have = toRoman(number);

    // assert
    expect(have).toEqual(want);
  });

  it('should convert 35 to XXXV', () => {
    // arrange
    const number = 35;

    // act
    const want = 'XXXV';
    const have = toRoman(number);

    // assert
    expect(have).toEqual(want);
  });

  it('should convert 36 to XXXVI', () => {
    // arrange
    const number = 36;

    // act
    const want = 'XXXVI';
    const have = toRoman(number);

    // assert
    expect(have).toEqual(want);
  });

  it('should convert 37 to XXXVII', () => {
    // arrange
    const number = 37;

    // act
    const want = 'XXXVII';
    const have = toRoman(number);

    // assert
    expect(have).toEqual(want);
  });

  it('should convert 38 to XXXVIII', () => {
    // arrange
    const number = 38;

    // act
    const want = 'XXXVIII';
    const have = toRoman(number);

    // assert
    expect(have).toEqual(want);
  });

  it('should convert 39 to XXXIX', () => {
    // arrange
    const number = 39;

    // act
    const want = 'XXXIX';
    const have = toRoman(number);

    // assert
    expect(have).toEqual(want);
  });

  it('should convert 40 to XL', () => {
    // arrange
    const number = 40;

    // act
    const want = 'XL';
    const have = toRoman(number);

    // assert
    expect(have).toEqual(want);
  });

  it('should convert 41 to XLI', () => {
    // arrange
    const number = 41;

    // act
    const want = 'XLI';
    const have = toRoman(number);

    // assert
    expect(have).toEqual(want);
  });

  it('should convert 42 to XLII', () => {
    // arrange
    const number = 42;

    // act
    const want = 'XLII';
    const have = toRoman(number);

    // assert
    expect(have).toEqual(want);
  });

  it('should convert 43 to XLIII', () => {
    // arrange
    const number = 43;

    // act
    const want = 'XLIII';
    const have = toRoman(number);

    // assert
    expect(have).toEqual(want);
  });

  it('should convert 44 to XLIV', () => {
    // arrange
    const number = 44;

    // act
    const want = 'XLIV';
    const have = toRoman(number);

    // assert
    expect(have).toEqual(want);
  });

  it('should convert 45 to XLV', () => {
    // arrange
    const number = 45;

    // act
    const want = 'XLV';
    const have = toRoman(number);

    // assert
    expect(have).toEqual(want);
  });

  it('should convert 46 to XLVI', () => {
    // arrange
    const number = 46;

    // act
    const want = 'XLVI';
    const have = toRoman(number);

    // assert
    expect(have).toEqual(want);
  });

  it('should convert 47 to XLVII', () => {
    // arrange
    const number = 47;

    // act
    const want = 'XLVII';
    const have = toRoman(number);

    // assert
    expect(have).toEqual(want);
  });

  it('should convert 48 to XLVIII', () => {
    // arrange
    const number = 48;

    // act
    const want = 'XLVIII';
    const have = toRoman(number);

    // assert
    expect(have).toEqual(want);
  });

  it('should convert 49 to XLIX', () => {
    // arrange
    const number = 49;

    // act
    const want = 'XLIX';
    const have = toRoman(number);

    // assert
    expect(have).toEqual(want);
  });

  it('should convert 50 to L', () => {
    // arrange
    const number = 50;

    // act
    const want = 'L';
    const have = toRoman(number);

    // assert
    expect(have).toEqual(want);
  });

  it('should convert 51 to LI', () => {
    // arrange
    const number = 51;

    // act
    const want = 'LI';
    const have = toRoman(number);

    // assert
    expect(have).toEqual(want);
  });

  it('should convert 52 to LII', () => {
    // arrange
    const number = 52;

    // act
    const want = 'LII';
    const have = toRoman(number);

    // assert
    expect(have).toEqual(want);
  });

  it('should convert 53 to LIII', () => {
    // arrange
    const number = 53;

    // act
    const want = 'LIII';
    const have = toRoman(number);

    // assert
    expect(have).toEqual(want);
  });

  it('should convert 54 to LIV', () => {
    // arrange
    const number = 54;

    // act
    const want = 'LIV';
    const have = toRoman(number);

    // assert
    expect(have).toEqual(want);
  });

  it('should convert 55 to LV', () => {
    // arrange
    const number = 55;

    // act
    const want = 'LV';
    const have = toRoman(number);

    // assert
    expect(have).toEqual(want);
  });

  it('should convert 56 to LVI', () => {
    // arrange
    const number = 56;

    // act
    const want = 'LVI';
    const have = toRoman(number);

    // assert
    expect(have).toEqual(want);
  });

  it('should convert 57 to LVII', () => {
    // arrange
    const number = 57;

    // act
    const want = 'LVII';
    const have = toRoman(number);

    // assert
    expect(have).toEqual(want);
  });

  it('should convert 58 to LVIII', () => {
    // arrange
    const number = 58;

    // act
    const want = 'LVIII';
    const have = toRoman(number);

    // assert
    expect(have).toEqual(want);
  });

  it('should convert 59 to LIX', () => {
    // arrange
    const number = 59;

    // act
    const want = 'LIX';
    const have = toRoman(number);

    // assert
    expect(have).toEqual(want);
  });

  it('should convert 60 to LX', () => {
    // arrange
    const number = 60;

    // act
    const want = 'LX';
    const have = toRoman(number);

    // assert
    expect(have).toEqual(want);
  });

  it('should convert 61 to LXI', () => {
    // arrange
    const number = 61;

    // act
    const want = 'LXI';
    const have = toRoman(number);

    // assert
    expect(have).toEqual(want);
  });

  it('should convert 62 to LXII', () => {
    // arrange
    const number = 62;

    // act
    const want = 'LXII';
    const have = toRoman(number);

    // assert
    expect(have).toEqual(want);
  });

  it('should convert 63 to LXIII', () => {
    // arrange
    const number = 63;

    // act
    const want = 'LXIII';
    const have = toRoman(number);

    // assert
    expect(have).toEqual(want);
  });

  it('should convert 64 to LXIV', () => {
    // arrange
    const number = 64;

    // act
    const want = 'LXIV';
    const have = toRoman(number);

    // assert
    expect(have).toEqual(want);
  });

  it('should convert 65 to LXV', () => {
    // arrange
    const number = 65;

    // act
    const want = 'LXV';
    const have = toRoman(number);

    // assert
    expect(have).toEqual(want);
  });

  it('should convert 66 to LXVI', () => {
    // arrange
    const number = 66;

    // act
    const want = 'LXVI';
    const have = toRoman(number);

    // assert
    expect(have).toEqual(want);
  });

  it('should convert 67 to LXVII', () => {
    // arrange
    const number = 67;

    // act
    const want = 'LXVII';
    const have = toRoman(number);

    // assert
    expect(have).toEqual(want);
  });

  it('should convert 68 to LXVIII', () => {
    // arrange
    const number = 68;

    // act
    const want = 'LXVIII';
    const have = toRoman(number);

    // assert
    expect(have).toEqual(want);
  });

  it('should convert 69 to LXIX', () => {
    // arrange
    const number = 69;

    // act
    const want = 'LXIX';
    const have = toRoman(number);

    // assert
    expect(have).toEqual(want);
  });

  it('should convert 70 to LXX', () => {
    // arrange
    const number = 70;

    // act
    const want = 'LXX';
    const have = toRoman(number);

    // assert
    expect(have).toEqual(want);
  });

  it('should convert 71 to LXXI', () => {
    // arrange
    const number = 71;

    // act
    const want = 'LXXI';
    const have = toRoman(number);

    // assert
    expect(have).toEqual(want);
  });

  it('should convert 72 to LXXII', () => {
    // arrange
    const number = 72;

    // act
    const want = 'LXXII';
    const have = toRoman(number);

    // assert
    expect(have).toEqual(want);
  });

  it('should convert 73 to LXXIII', () => {
    // arrange
    const number = 73;

    // act
    const want = 'LXXIII';
    const have = toRoman(number);

    // assert
    expect(have).toEqual(want);
  });

  it('should convert 74 to LXXIV', () => {
    // arrange
    const number = 74;

    // act
    const want = 'LXXIV';
    const have = toRoman(number);

    // assert
    expect(have).toEqual(want);
  });

  it('should convert 75 to LXXV', () => {
    // arrange
    const number = 75;

    // act
    const want = 'LXXV';
    const have = toRoman(number);

    // assert
    expect(have).toEqual(want);
  });

  it('should convert 76 to LXXVI', () => {
    // arrange
    const number = 76;

    // act
    const want = 'LXXVI';
    const have = toRoman(number);

    // assert
    expect(have).toEqual(want);
  });

  it('should convert 77 to LXXVII', () => {
    // arrange
    const number = 77;

    // act
    const want = 'LXXVII';
    const have = toRoman(number);

    // assert
    expect(have).toEqual(want);
  });

  it('should convert 78 to LXXVIII', () => {
    // arrange
    const number = 78;

    // act
    const want = 'LXXVIII';
    const have = toRoman(number);

    // assert
    expect(have).toEqual(want);
  });

  it('should convert 79 to LXXIX', () => {
    // arrange
    const number = 79;

    // act
    const want = 'LXXIX';
    const have = toRoman(number);

    // assert
    expect(have).toEqual(want);
  });

  it('should convert 80 to LXXX', () => {
    // arrange
    const number = 80;

    // act
    const want = 'LXXX';
    const have = toRoman(number);

    // assert
    expect(have).toEqual(want);
  });

  it('should convert 81 to LXXXI', () => {
    // arrange
    const number = 81;

    // act
    const want = 'LXXXI';
    const have = toRoman(number);

    // assert
    expect(have).toEqual(want);
  });

  it('should convert 82 to LXXXII', () => {
    // arrange
    const number = 82;

    // act
    const want = 'LXXXII';
    const have = toRoman(number);

    // assert
    expect(have).toEqual(want);
  });

  it('should convert 83 to LXXXIII', () => {
    // arrange
    const number = 83;

    // act
    const want = 'LXXXIII';
    const have = toRoman(number);

    // assert
    expect(have).toEqual(want);
  });

  it('should convert 84 to LXXXIV', () => {
    // arrange
    const number = 84;

    // act
    const want = 'LXXXIV';
    const have = toRoman(number);

    // assert
    expect(have).toEqual(want);
  });

  it('should convert 85 to LXXXV', () => {
    // arrange
    const number = 85;

    // act
    const want = 'LXXXV';
    const have = toRoman(number);

    // assert
    expect(have).toEqual(want);
  });

  it('should convert 86 to LXXXVI', () => {
    // arrange
    const number = 86;

    // act
    const want = 'LXXXVI';
    const have = toRoman(number);

    // assert
    expect(have).toEqual(want);
  });

  it('should convert 87 to LXXXVII', () => {
    // arrange
    const number = 87;

    // act
    const want = 'LXXXVII';
    const have = toRoman(number);

    // assert
    expect(have).toEqual(want);
  });

  it('should convert 88 to LXXXVIII', () => {
    // arrange
    const number = 88;

    // act
    const want = 'LXXXVIII';
    const have = toRoman(number);

    // assert
    expect(have).toEqual(want);
  });

  it('should convert 89 to LXXXIX', () => {
    // arrange
    const number = 89;

    // act
    const want = 'LXXXIX';
    const have = toRoman(number);

    // assert
    expect(have).toEqual(want);
  });

  it('should convert 90 to XC', () => {
    // arrange
    const number = 90;

    // act
    const want = 'XC';
    const have = toRoman(number);

    // assert
    expect(have).toEqual(want);
  });

  it('should convert 91 to XCI', () => {
    // arrange
    const number = 91;

    // act
    const want = 'XCI';
    const have = toRoman(number);

    // assert
    expect(have).toEqual(want);
  });

  it('should convert 92 to XCII', () => {
    // arrange
    const number = 92;

    // act
    const want = 'XCII';
    const have = toRoman(number);

    // assert
    expect(have).toEqual(want);
  });

  it('should convert 93 to XCIII', () => {
    // arrange
    const number = 93;

    // act
    const want = 'XCIII';
    const have = toRoman(number);

    // assert
    expect(have).toEqual(want);
  });

  it('should convert 94 to XCIV', () => {
    // arrange
    const number = 94;

    // act
    const want = 'XCIV';
    const have = toRoman(number);

    // assert
    expect(have).toEqual(want);
  });

  it('should convert 95 to XCV', () => {
    // arrange
    const number = 95;

    // act
    const want = 'XCV';
    const have = toRoman(number);

    // assert
    expect(have).toEqual(want);
  });

  it('should convert 96 to XCVI', () => {
    // arrange
    const number = 96;

    // act
    const want = 'XCVI';
    const have = toRoman(number);

    // assert
    expect(have).toEqual(want);
  });

  it('should convert 97 to XCVII', () => {
    // arrange
    const number = 97;

    // act
    const want = 'XCVII';
    const have = toRoman(number);

    // assert
    expect(have).toEqual(want);
  });

  it('should convert 98 to XCVIII', () => {
    // arrange
    const number = 98;

    // act
    const want = 'XCVIII';
    const have = toRoman(number);

    // assert
    expect(have).toEqual(want);
  });

  it('should convert 99 to XCIX', () => {
    // arrange
    const number = 99;

    // act
    const want = 'XCIX';
    const have = toRoman(number);

    // assert
    expect(have).toEqual(want);
  });

  it('should convert 100 to C', () => {
    // arrange
    const number = 100;

    // act
    const want = 'C';
    const have = toRoman(number);

    // assert
    expect(have).toEqual(want);
  });

  it('should convert 200 to CC', () => {
    // arrange
    const number = 200;

    // act
    const want = 'CC';
    const have = toRoman(number);

    // assert
    expect(have).toEqual(want);
  });

  it('should convert 300 to CCC', () => {
    // arrange
    const number = 300;

    // act
    const want = 'CCC';
    const have = toRoman(number);

    // assert
    expect(have).toEqual(want);
  });

  it('should convert 400 to CD', () => {
    // arrange
    const number = 400;

    // act
    const want = 'CD';
    const have = toRoman(number);

    // assert
    expect(have).toEqual(want);
  });

  it('should convert 500 to D', () => {
    // arrange
    const number = 500;

    // act
    const want = 'D';
    const have = toRoman(number);

    // assert
    expect(have).toEqual(want);
  });

  it('should convert 600 to DC', () => {
    // arrange
    const number = 600;

    // act
    const want = 'DC';
    const have = toRoman(number);

    // assert
    expect(have).toEqual(want);
  });

  it('should convert 700 to DCC', () => {
    // arrange
    const number = 700;

    // act
    const want = 'DCC';
    const have = toRoman(number);

    // assert
    expect(have).toEqual(want);
  });

  it('should convert 800 to DCCC', () => {
    // arrange
    const number = 800;

    // act
    const want = 'DCCC';
    const have = toRoman(number);

    // assert
    expect(have).toEqual(want);
  });

  it('should convert 900 to CM', () => {
    // arrange
    const number = 900;

    // act
    const want = 'CM';
    const have = toRoman(number);

    // assert
    expect(have).toEqual(want);
  });

  it('should convert 1000 to M', () => {
    // arrange
    const number = 1000;

    // act
    const want = 'M';
    const have = toRoman(number);

    // assert
    expect(have).toEqual(want);
  });

  it('should convert 4999 (Maximum) to MMMMCMXCIX', () => {
    // arrange
    const number = 4999;

    // act
    const want = 'MMMMCMXCIX';
    const have = toRoman(number);

    // assert
    expect(have).toEqual(want);
  });

  it('should fail on negative.', () => {
    // arrange
    const number = -1;

    // assert
    expect(() => {
      toRoman(number);
    }).toThrow(RangeError);
    expect(() => {
      toRoman(number);
    }).toThrow('(arg): number must be non-negativ.');
  });

  it('should fail on zero.', () => {
    // arrange
    const number = 0;

    // assert
    expect(() => {
      toRoman(number);
    }).toThrow(RangeError);
    expect(() => {
      toRoman(number);
    }).toThrow('(arg): number must not be zero.');
  });

  it('should fail if maximum value (4999) is exceeded.', () => {
    // arrange
    const number = 5000;

    // assert
    expect(() => {
      toRoman(number);
    }).toThrow(RangeError);
    expect(() => {
      toRoman(number);
    }).toThrow('(arg): number must be less than or equal to 4999.');
  });
});
